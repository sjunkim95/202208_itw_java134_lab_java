/* 
조인(join): 2개 이상의 테이블에서 검색하는 것.

join 문법:
1) ANSI 표준 문법:
    select 컬럼, ...
    from 테이블1 
        join종류 테이블2 on join조건;
2) Oracle 문법:
    select 컬럼, ...
    from 테이블1, 테이블2, ...
    where join조건;
*/
